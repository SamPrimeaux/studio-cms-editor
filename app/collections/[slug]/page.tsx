import Link from "next/link";
import { StoreFooter, StoreHeader } from "../../components/Storefront";
import { archetypes, products } from "../../storefront-data";
export default async function Page({params}:{params:Promise<{slug:string}>}){ const {slug}=await params; const animal=archetypes.find(a=>a.name.toLowerCase()===slug)??archetypes[0]; return <main className="ia-site"><StoreHeader/><section className="ia-page-hero"><span>ARCHETYPE / {animal.name.toUpperCase()}</span><h1>{animal.name}<br/>FIELD.</h1><p>{animal.line}. A behavioral collection built around how you move through the world.</p></section><section className="ia-shop"><div className="ia-product-grid">{products.map(p=><article className="ia-product-card" key={p.slug}><Link className="ia-product-image" href={`/product/${p.slug}`} style={{backgroundImage:`url(${p.image})`}}><span>{p.category}</span><em>View field spec →</em></Link><div><p>{p.name}</p><span>${p.price}</span></div><small>{p.fit} / {p.weight}</small></article>)}</div></section><StoreFooter/></main> }
